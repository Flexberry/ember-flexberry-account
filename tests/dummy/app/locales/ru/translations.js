import Ember from 'ember';
import emberFlexberryAccountTranslations from 'ember-flexberry-account/locales/ru/translations';

const translations = {};
Ember.$.extend(true, translations, emberFlexberryAccountTranslations);

Ember.$.extend(true, translations, {

});

export default translations;
