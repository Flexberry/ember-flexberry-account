import Ember from 'ember';
import emberFlexberryAccountTranslations from 'ember-flexberry-account/locales/en/translations';

const translations = {};
Ember.$.extend(true, translations, emberFlexberryAccountTranslations);

Ember.$.extend(true, translations, {

});

export default translations;
